# 수열
a = 시작 값
m = 곱할 값
d = 더할값
n = 몇 번째 인지를 나타내는 정수
(a,m,d 는 -50 ~+50, n은 10이하의 자연수)

```python
def sequences(a, m, d, n):
    for i in range(a, n):
       a = a * m + d
    return a

a, m, d, n= map(int, input().split())
result = sequences(a, m, d, n)

print(result)
```
